languageCode = 'en-us'

title = 'build-test'

cleanDestinationDir = true

[outputs]
home = ["HTML", "RSS"]
section = ["HTML", "RSS"]
taxonomy = ["HTML"]
term = ["HTML"]
page = ["HTML"]

[markup.goldmark.renderer]
  unsafe = true